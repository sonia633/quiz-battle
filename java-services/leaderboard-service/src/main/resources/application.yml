server:
  port: 8082

spring:
  application:
    name: leaderboard-service
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/quizbattle}
    username: ${DB_USERNAME:quizbattle}
    password: ${DB_PASSWORD:quizbattle}
  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC

springdoc:
  swagger-ui:
    path: /swagger-ui.html

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    com.quizbattle.leaderboard: INFO
